@model List<Event>
@{
    ViewData["Title"] = "Index";
}


<div class="row">
    <div class="col-lg-10 offset-lg-1 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Məhsullar</h4>

                <a asp-action="Create" class="btn btn-primary mt-3">YENİSİNİ YARAT</a>

                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>
                                   Image
                                </th>
                                <th>
                                    Description
                                </th>
                                <th>
                                   Title
                                </th>
                                <th>
                                   Time
                                </th>
                                <th>
                                   Settings
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Event item in Model)
                            {
                                <tr>
                                    <td class="text-center">
                                        <img src="~/img/@item.Image" alt="#" style="width:100px; height:100px" />
                                    </td>
                                    <td class="text-center">
                                        @item.Description
                                    </td>
                                    <td class="text-center">
                                        @item.Title
                                    </td>
                                    <td class="text-center">
                                        @item.Time
                                    </td>
                                    <td class="text-center">
                                        <a asp-action="Detail" asp-route-id="@item.Id" class="btn btn-success">
                                            <i class="mdi mdi-account-card-details"></i>
                                        </a>
                                        <a asp-action="Update" asp-route-id="@item.Id" class="btn btn-warning">
                                            <i class="mdi mdi-table-edit"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">
                                            <i class="mdi mdi-delete"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

            </div>

        </div>

    </div>
</div>

